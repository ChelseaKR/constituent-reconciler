# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Run outputs (never commit resolved records or review queues)
out/
out-dv/
*.out.csv

# Generated synthetic corpora (FIX-11): deterministic from the seed baked
# into tools/corpusgen, so the raw CSVs are reproducible and not committed.
# Only the derived report (eval/large-corpus-report.md) is committed.
eval/large-corpus/

# Node (accessibility audit tooling only; see docs/decisions/0009)
node_modules/
.axe-fixtures/

# OS
.DS_Store
.coverage
